import 'classes.dart' as my_shapes; // you can use hide to specifically not show something, and as such, the opposite of this is show
// pub.dev imports work the same way

class Circle {}

void main() {
  my_shapes.Circle(radius: 10);
}